# HFSSagent Project Notes

## 项目简介

本项目目标是构建一个本地 HFSS 操作 agent MCP 服务。用户用自然语言描述 HFSS 相关任务后，agent 能理解任务意图，并利用 MCP 操作本地 Ansys HFSS 完成建模画图、参数设置、仿真运行、结果提取与后续分析。项目的核心定位不是让小模型临时编写复杂 PyAEDT/HFSS 脚本，而是把 HFSS 设计与验证能力沉淀成可发现、可组合、可校验的 MCP 能力：小模型先阅读 MCP resources 中的天线设计经验、流程模板、工具边界和验收规则，再选择并拼接受控动作积木完成设计、仿真、验证、结果分析和必要的迭代。通过这种方式，本地小模型也能完成天线设计，或自动复现论文设计并验证，而无需依赖强大大模型一次性生成完整且正确的自动化脚本。


## git维护规则

1. commit的时候避免大文件

## 本地模型、大文件下载路径

1. 默认放到工作项目路径上，如果可以多项目公用，可以独立与工作项目路径新建文件夹放入

## 完成代码需求的标准

1. 从0到1构建项目的时候，一定要遵从需求
2. 注意代码的可拓展性，注意模块的解耦，方便后续拓展新功能，或者修改功能的时候不会影响到太多代码
3. 每次代码都要先跑通了再汇报，永远不要交付一个用户没办法跑起来的产品

## 长期项目开发偏好

1. 每次开发前先阅读项目知识、现有 docs 文档和相关调研资料，确认没有偏离“自然语言 agent 控制本地/服务器 HFSS”的项目目的。
2. 项目初始阶段必须有清晰的基础文档，至少包括项目需求、架构设计和计划路线。已有文档要作为后续开发的约束来源，不能在开发中偏离这些目标。
3. 代码开发阶段，每个较大的功能模块都要有对应的模块文档，写清楚该模块的目标、边界、核心接口、数据流、验证方式和已知限制。
4. 版本交付阶段，要维护面向整体项目的交付文档，说明所有主要模块之间的关系、项目整体数据流向、部署方式、运行方式和典型用法。
5. 后续迭代时，修改已有模块必须同步更新该模块文档；新增模块必须新增对应文档；废弃或重构模块时，要更新或删除过期文档，避免文档和代码不一致。
6. 文档统一放在项目根目录的 `docs` 文件夹。一个模块一个文档，文件名保持简洁；文档内容以最新事实为准，不堆砌历史过程。
7. 当前真实 HFSS 环境为 Ansys Electronics Desktop Student 2025 R2。后续涉及 HFSS 能力、PyAEDT 后端、MCP 服务联调或模块交付验证时，除非用户另有说明，必须优先启动真实 MCP 服务并在真实 HFSS 中做 smoke test；离线 mock 测试只能作为基础验证，不能替代真实 HFSS 验证。
8. 测试中发现的严重问题，如果代表某个模块需要升级或架构能力需要补齐，必须在 `docs/测试问题` 文件夹下建立单独问题文档。一个文件只记录一个重大问题，内容包括问题现象、复现方式、原因分析、影响范围、需要修改的模块、计划改动、改动后的验证结果和当前状态。后续修改相关模块时，必须同步维护该问题文档和对应模块文档。
9. 测试发现问题后，默认不只是报告问题，而是先自行进行系统性定位和修正，持续推进到用户当前指令的目的完成，或对应模块的功能目标完成。只有在外部环境、权限、真实 HFSS 行为或用户决策阻塞导致无法继续时，才向用户报告阻塞点、已验证证据和下一步建议。
10. 后续涉及 HFSS 建模、仿真、验证或资源释放的开发，必须吸取 2026-07-23 真实测试暴露的问题：第一次测试不能只证明 agent 能调用工具，而要证明用户真实工作流可用，包括 GUI 是否可见且可人工接管、默认断开连接是否保存并关闭工程与 AEDT 进程、明确要求保留窗口时是否只释放 MCP 控制权且保留 GUI。
11. HFSS 仿真流程必须把 `validate_design` 作为求解前门禁。标准流程应按“建模、材料、边界、端口/激励、airbox/辐射区域、setup/sweep、validate、solve、读取结果、保存/释放资源”推进；validation 不通过时不得进入求解，必须把 HFSS 原始 validation 消息返回给 agent 和用户。
12. 后续新增或修改天线 workflow 时，不能只验证几何对象数量。最低验收必须覆盖材料、金属/辐射等边界、端口与积分线、求解区域、setup/sweep、真实 HFSS validation 消息和资源释放结果；至少包含一个正向 validation smoke test 和一个故意失败场景，确认 agent 能看到真实 HFSS 报错。
13. 真实 HFSS 后端异常处理必须优先返回工程软件的真实诊断信息，而不只是 Python traceback。能读取 AEDT message manager、PyAEDT logger 或 solver log 时，应将原始错误作为结构化字段返回；traceback 只作为开发调试信息保留。
14. 对 HFSS/AEDT 这类闭源长会话桌面软件，不能只相信 API 返回成功。涉及 release、close、disconnect、solve 等操作时，必须用外部可观察证据验证结果，例如 AEDT PID 是否退出、工程文件锁是否释放、GUI 截图是否真实来自 HFSS 窗口、message manager 是否给出预期消息。
15. 后续项目演进必须优先服务“小模型可用性”：MCP tool 不应退化为 PyAEDT API 的薄包装，也不应要求 agent 生成或提交任意脚本；应优先提供领域语义明确的工具、resources、prompt/workflow 模板、结构化状态摘要、下一步建议、失败诊断和 validation gate，让小模型通过选择、规划和组合完成任务。
16. 后续新增 resource 时，应把它视为小模型的领域工作手册，而不是普通说明文档。每个 resource 应尽量包含适用场景、前置条件、推荐工具序列、关键参数含义、常见 HFSS 报错、验证标准和失败后的恢复路径。
17. 后续每次发布 GitHub release 时，必须同步发布 Windows 离线运行包。release 不能只包含源码或说明；除非用户明确要求跳过离线包，否则需要运行离线打包脚本、验证压缩包可导入并启动 MCP 服务、把离线 zip 作为 release asset 上传，并在 release notes 中写明离线包名称、构建 commit、Python 版本和基本验证结果。
