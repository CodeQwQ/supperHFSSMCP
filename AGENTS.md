# HFSSagent Project Notes

## 项目简介

本项目目标是构建一个本地 HFSS 操作 agent。用户用自然语言描述 HFSS 相关任务后，agent 能理解任务意图，并辅助操作本地 Ansys HFSS 完成建模画图、参数设置、仿真运行、结果提取与后续分析。

当前阶段先记录项目方向；后续用户会继续补充任务类型、HFSS 操作细节、自动化边界、交互方式、仿真流程、结果校验标准等需求。

## git维护规则

1. commit的时候避免大文件

## 本地模型、大文件下载路径

1. 默认放到工作项目路径上，如果可以多项目公用，可以独立与工作项目路径新建文件夹放入

## 完成代码需求的标准

1. 从0到1构建项目的时候，一定要遵从需求
2. 注意代码的可拓展性，注意模块的解耦，方便后续拓展新功能，或者修改功能的时候不会影响到太多代码
3. 每次代码都要先跑通了再汇报，永远不要交付一个用户没办法跑起来的产品

## 长期项目开发偏好

1. 每次开发前先阅读项目知识、现有 docs 文档和相关调研资料，确认没有偏离“自然语言 agent 控制本地/服务器 HFSS”的项目目的。
2. 大型开发完成后必须新增或更新对应文档。架构类变更要说明架构版本、日期、架构图、模块描述和设计原因。
3. 文档统一放在项目根目录的 `docs` 文件夹。一个模块一个文档，文件名保持简洁；旧文档要及时更新，废弃内容要删除，缺失内容要补充。
