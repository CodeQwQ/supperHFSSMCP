# 天线设计信息理解 MCP 真实链路验收报告

验收日期：2026-07-29。执行者：独立验收 agent。目标是验证场景三人工证据能够交接到现有 HFSS MCP，并确认 HFSS 在 `validate_design` 通过后实际进入 solver。

## 输入与规格交接

论文只读输入：`E:\陈威-毕设\代码\天线拓扑优化\docs\相关论文\Machine-Learning-Assisted_Optimization_for_Antenna_Geometry_Design.pdf`（文件存在，大小 2,523,323 bytes）。首版使用 `VerificationEvidenceProvider` 的人工核对路径，不下载 OCR/VLM；服务单元测试已验证场景三字段可生成 `AntennaDesignSpec`：5.725–5.825 GHz、εr=4.6、tanδ=0.001、中心距 12.9 mm，并保留端口积分线、辐射区域和去耦轮廓为 unresolved，避免猜测。

## 真实 MCP/HFSS 执行

服务命令（已有服务，未修改源码）：

```powershell
$env:PYTHONPATH='src'
\.venv\Scripts\python.exe -B -m hfss_agent_mcp run --backend pyaedt --transport streamable-http --host 127.0.0.1 --port 8060
```

真实 AEDT：`D:\Ansys\ANSYS Inc\ANSYS Student\v252\AnsysEM\ansysedtsv.exe -grpcsrv 50121`，GUI 模式（`non_graphical=false`），PID 37976；MCP endpoint 为 `http://127.0.0.1:8060/mcp`。

按顺序调用：`connect_hfss → create_hfss_design → create_dipole_antenna → create_simulation_setup → validate_design → run_simulation → get_simulation_job → release_connection`。本轮用现有受控 dipole workflow 作为 HFSS 交接 smoke test；场景三的 MIMO 去耦几何不能在缺少轮廓证据时臆造。

## 关键证据

- `connect_hfss`：返回 `status=ok`，AEDT 2025.2SV Student，项目 `Project19`，设计 `HFSS_GUI_Acceptance_20260723`。
- `validate_design` 在求解前执行并返回 `valid=true`、`validation_backend=pyaedt`、`api=validate_full_design`。原始 HFSS 消息包含 `Design validation check PASSED.`、`Detected setup and sweep: Setup1 |__ Sweep1`；另有端口阻抗重归一化 warning。
- `run_simulation` 返回真实提交信息：`backend_note=Real HFSS solver execution was submitted`，且携带同一次 validation 结果。
- `get_simulation_job(job-0001)` 返回 `status=completed`、`log_summary=Backend solve finished`、`failure_reason=null`；`simulation_status_checks=22`，其中 21 次 `running=true`、最后一次 `running=false`，因此确认 solver 实际运行而非仅创建 job 记录。
- `release_connection(session_id=pyaedt-0001, save_project=false)` 返回 `release_result=true`、`close_desktop=true`、`aedt_process_id=37976`、`process_closed=true`。释放后等待 5 秒，PID 37976 不再存在；仅 Ansys licensing client 进程保留。
- MCP 原始服务日志：`tmp/hfss2.err`，记录 Uvicorn 监听 8060、MCP 请求、session 创建/终止（11:15:52–11:18:07）。

## 结论与边界

本轮通过“人工证据 provider → 结构化规格字段 → 现有 HFSS MCP → validation gate → 真实 solver → 可观察释放”的组合验收。真实 HFSS smoke test 通过。由于场景三的完整去耦结构轮廓、端口积分线和辐射区域尺寸未被证据 provider 确认，本报告不宣称论文场景三几何已完成复现；下一步必须补充这些证据后再执行 MIMO 专项验收。

离线环境中仍应使用部署指南配置具体 OCR/VLM provider；provider 替换不得改变 `AntennaDesignSpec` schema 或 HFSS MCP 接口。
