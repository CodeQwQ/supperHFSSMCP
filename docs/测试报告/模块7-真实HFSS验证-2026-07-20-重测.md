# 模块 7 真实 HFSS 验收重测报告

## 1. 结论

- **核心链路：PASS**：真实 streamable HTTP MCP、MCP ClientSession、脚本发现、HFSS gRPC 连接、PyAEDT runner 和 native runner 均有成功返回。
- **COM runner：环境限制**：Student 环境中两个候选 COM ProgID 均返回真实“无效的类字符串”，没有伪造成功，也没有生成 artifact。
- **严格本轮指定条件：FAIL（证据不完整）**：已有证据文件中的 request_id 是 `module7-rerun-1784525785`，不是指定的 `module7-retest`；已有三张窗口截图也未捕获有效的 HFSS AEDT 工作区。

因此，本报告不把本轮记为“严格条件全部通过”；在已保存证据范围内，模块 7 的核心功能通过，COM 属于 Student 环境限制。

## 2. 验证环境

| 项目 | 实际值 |
|---|---|
| MCP endpoint | `http://127.0.0.1:8042/mcp` |
| MCP transport | `streamable-http` |
| MCP client | `mcp.ClientSession` + `streamable_http_client` |
| AEDT | Ansys Electronics Desktop Student 2025 R2 |
| gRPC 端口 | `53387`、`53388` 均连接成功 |
| project | `Project14` |
| design | `HFSS_Module6_Verifier10` |
| project path | `D:\ansysProjects\Project14.aedt` |
| object count | `6` |

## 3. MCP 调用链

已有 JSON 记录了同一 HTTP MCP 会话中的：

1. `initialize`：PASS，协议版本 `2025-11-25`。
2. `tools/list`：PASS，返回工具列表，包含 `list_automation_scripts` 和 `run_automation_script`。
3. `list_automation_scripts`：PASS，发现服务器登记脚本 `aedt_probe`。
4. `connect_hfss`：PASS，`53387` 和 `53388` 均返回 `status="ok"`，读取到 `Project14 / HFSS_Module6_Verifier10`。
5. `run_automation_script`：分别执行 `pyaedt`、`native`、`com`。

## 4. Runner 结果

### 4.1 PyAEDT

**PASS。**

- 返回 `status="ok"`。
- `return_code=0`。
- 真实命令连接 Student AEDT gRPC `port=53387`。
- artifact：

```json
{
  "project_name": "Project14",
  "design_name": "HFSS_Module6_Verifier10",
  "script_id": "aedt_probe",
  "arguments": {
    "runner": "pyaedt",
    "request_id": "module7-rerun-1784525785"
  },
  "success": true
}
```

artifact 已包含真实 project/design、`script_id` 和 arguments。注意：已有证据中的 request_id 与本轮指定值 `module7-retest` 不一致。

### 4.2 Native

**PASS（runner 子项）。**

- 真实 `ansysedtsv.exe -RunScriptAndExit` 返回 `return_code=0`。
- artifact 中包含 `script_id=aedt_probe`、runner 和 request_id。
- native CLI 运行上下文没有返回 project/design 名称，因此不将 native artifact 单独作为项目状态探测证据。

### 4.3 COM

**环境限制，不伪造成功。**

MCP 返回：

```text
Unable to attach to active AEDT COM application: No active AEDT COM object was found.
Tried Ansoft.ElectronicsDesktop: (-2147221005, '无效的类字符串', ...)
Tried Ansoft.ElectronicsDesktop.2025.2: (-2147221005, '无效的类字符串', ...)
```

未进入 `RunScript`，未生成 artifact。该结果判定为 Student 安装/COM 注册能力限制，不得改判为成功。

## 5. 截图证据

已有证据目录中的三张窗口截图已读取：

- [02-hfss-window-5838398.png](E:/LLMproject/HFSSagent/docs/测试报告/证据/模块7-重测/02-hfss-window-5838398.png)
- [03-hfss-window-594754.png](E:/LLMproject/HFSSagent/docs/测试报告/证据/模块7-重测/03-hfss-window-594754.png)
- [04-hfss-window-22282284.png](E:/LLMproject/HFSSagent/docs/测试报告/证据/模块7-重测/04-hfss-window-22282284.png)

三张图显示的是 Windows 桌面/锁屏背景，没有可识别的 HFSS AEDT 工作区，因此只能作为“截图尝试”留档，不能作为 HFSS 窗口状态证据。

## 6. 原始证据

- [MCP 完整返回 JSON](E:/LLMproject/HFSSagent/docs/测试报告/证据/模块7-重测/mcp-http-full-rerun.json)
- [本轮客户端采集脚本](E:/LLMproject/HFSSagent/docs/测试报告/证据/模块7-重测/client_verifier_module7_retest.py)

## 7. 验收表

| 验收项 | 结论 |
|---|---|
| MCP HTTP / ClientSession | PASS |
| initialize / tools/list | PASS |
| list_automation_scripts | PASS |
| connect_hfss / 53387 | PASS |
| connect_hfss / 53388 | PASS |
| PyAEDT artifact | PASS |
| Native exit 0 / artifact | PASS |
| COM | 环境限制：Student COM ProgID 未注册 |
| 指定 request_id=`module7-retest` | 未验证，已有 JSON 使用其他 request_id |
| 有效 HFSS 窗口截图 | FAIL，截图未捕获 AEDT 工作区 |
| 严格重测总体 | **FAIL（证据条件不完整）** |

